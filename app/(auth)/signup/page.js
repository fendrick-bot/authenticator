import { SignupForm } from "@/components/authComp/SignupForm/SignupForm";

export default function Signup() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <SignupForm />
    </div>
  );
}
