import ProfileDetails from "@/components/authComp/ProfileDetails/ProfileDetails";

export default function MyProfile() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <ProfileDetails/>
    </div>
  );
}
